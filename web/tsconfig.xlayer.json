{
  "//": "The forked app carries type errors of its own (dapp-kit and @mysten ship mismatched types across packages) and its build ignores them. The X Layer work does not get that pass: this config typechecks only what we added, and CI runs it.",
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": [
    "next-env.d.ts",
    "lib/xlayer/**/*.ts",
    "lib/xlayer/**/*.tsx",
    "app/xlayer/**/*.tsx",
    "app/api/zap/**/*.ts",
    "components/XLayerConnectPill.tsx"
  ]
}
